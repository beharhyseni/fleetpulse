# Copy to .env and fill in. Never commit .env.
# DATABASE_URL=postgresql+psycopg://fleetpulse:fleetpulse@localhost:5432/fleetpulse   # override if needed
ANTHROPIC_API_KEY=sk-ant-...   # powers GET /incidents/summary; endpoint returns 503 without it
# FLEETPULSE_API_KEY=change-me   # Phase 6+: when set, /incidents/* requires X-API-Key header
DEBUG=false
